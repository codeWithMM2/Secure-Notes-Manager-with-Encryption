# Secure-Notes-Manager-with-Encryption
A python application that encrypts and protects personal notes using character-level encryption and master password authentication.Requires password verification before every read and write operation with account lockout after 3 failed attempts.
